<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Item Details</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Item Details</h1>
    <% if (item) { %>
        <p><strong>Task:</strong> <%= item.name || "No task available" %></p>
    <% } else { %>
        <p>Item not found!</p>
    <% } %>
    <a href="/api/items">Go Back</a>
</body>
</html>
